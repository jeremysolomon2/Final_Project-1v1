<h2 class="text-center"><%= @court.name %> </h1>
<h4 class="text-center"><%= "#{@court.address}, #{@court.city}, #{@court.state}" %> </h1>
<div class="text-center pb-2">
        <%= button_to "Create Game", {controller: "games", action: "create", court_id: @court.id }, class: 'btn btn-primary' %>
</div>
<table class="table">
    <thead>
        <th>Player 1</th>
        <th></th>
        <th>Opponent</th>
        <th>Scorekeeper</th>
        <th></th>
    </thead> 
    <tbody>
        <% @games.each do |game| %>
            <tr>
                <td><%= game.user.nickname %></td>
                <td>vs</td>
                <% if game.opponent.nil? %>
                    <td><%= render 'games/set_opponent', game: game %></td>
                <% else %>
                     <td><%= game.opponent&.nickname %></td>
                <% end %>
                <% if game.score_keeper.nil? %>
                    <td><%= render 'games/set_scorekeeper', game: game %></td>
                <% else %>
                     <td><%= game.score_keeper&.nickname %></td>
                <% end %>
                <td><%= link_to "Go to game", game, class: "btn btn-primary" %></td>
            </tr>
        <% end %>
    </tbody>
</table>